# Executable Beacon Chain Local Network

```shell
git clone https://github.com/garyschulte/executable-beacon-chain-localnet.git
cd executable-beacon-chain-localnet && sh prep_volumes.sh && docker-compose up
```
